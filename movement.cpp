#include "movement.h"
/*

bool
movement::is_moving()
{
    sensors_event_t event;
    this->accel()->getEvent( &event );

    float x = event.acceleration.x;
    float y = event.acceleration.y;
    float z = event.acceleration.z; 
    
    return ( ( x + y  z ) ) - 9 > JIGGLE_FACTOR );
}

*/
